/**
 * Challenge
 * Fix the following code so that it outputs the correct amount.
 */

var moneyInBank = 100;
var moneyInWallet = "50";

var totalMoney = moneyInBank + moneyInWallet;

console.log("I have ", totalMoney, " dollars.");
