/**
 * Challenge
 * Complete the code below so that it runs.
 *
 * 1. Make a function called `getProduct`
 * 2. Parameter 1 is a number
 * 3. Parameter 2 is the multiplier
 * 4. Multiply the number by the multiplier
 * 5. Return the result
 */

console.log(getProduct(2, 3)); // 6
console.log(getProduct(4, 3)); // 12
console.log(getProduct(10, 10)); // 100
console.log(getProduct(6, 4)); // 24
