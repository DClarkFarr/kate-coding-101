/**
 * Fix the string below, so that the message is output correctly.
 * Note: I still want the double-quotes in the string!
 */

var message = "Please don't call me "crazy", Bob!";


console.log('message:', message);