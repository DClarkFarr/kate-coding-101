/**
 * Challenge
 * Fix the code so that it runs correctly.
 */

var firstName1 = 'daniel';
var firstName2 = 'kate';
var firstName3 = 'cassidy';

var makeFullName = function(firstName lastName){
  fullName = firstName + ' ' + lastName;

  return fulllllname
}

var listOfNames = [fullName1, fullName2, fullName3];

console.log(listOfNames);

var fullName1 = makeFullName(firstName1, lastNameAll);
var fullName2 = makeFullName(firstName2, lastNameAll);
var fullName3 = makeFullName(firstName3, lastNameAll);

