/**
 * Challenge
 * Fix the code so that it runs correctly.
 */

const firstName1 = 'daniel';
const firstName2 = 'kate';
const firstName3 = 'cassidy';

const makeFullName = function(firstName lastName){
  fullName = firstName + ' ' + lastName;

  return fulllllname
}

const listOfNames = [fullName1, fullName2, fullName3];

console.log(listOfNames);

const fullName1 = makeFullName(firstName1, lastNameAll);
const fullName2 = makeFullName(firstName2, lastNameAll);
const fullName3 = makeFullName(firstName3, lastNameAll);

