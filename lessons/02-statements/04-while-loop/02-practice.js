/**
 * FIX IT
 * Fix the following code so that outputs the correct total.
 * 100 + 200 + 300 = 600
 */

var amounts = [100, 200, 300];

var total = 0;

var c = -1;
while (c < amounts.length) {
  c++;

  total = amounts[1];
}
