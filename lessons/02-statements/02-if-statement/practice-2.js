/**
 * Challenge
 * Fix the code below so that it runs.
 *
 * User must have a username and password to log in.
 */

var username = "johndoe";
var password = "";

if (username || password) {
  console.log("Correct! Username and password were used!");
} else {
  console.log("Incorrect! Username or password were not used!");
}
// Password is empty, so we want to see the "Incorrect" message.
