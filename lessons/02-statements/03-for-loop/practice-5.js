/**
 * Do it
 * Create a counter loop;
 * Give it a start value of 15
 * Give it an end value of 45
 * Increment the counter with each iteration
 * After the loop, console.log the counter
 */

var counter = 0;
